<ul class="breadcrumb">
  <li class="active">新闻 - <%= @topic.title %><li>
</ul>

<%= content_tag(:pre, @topic.body) unless @topic.body.blank? %>

<%= link_to '修改', edit_admin_topic_path(@topic), class: 'btn' %>
<%= link_to '返回', admin_topics_path, class: 'btn' %>
