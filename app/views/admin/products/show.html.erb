<%= render 'shared/breadcrumb', title: "产品展示 - #{@product.name}" %>

<% if @product.image? %>
  <ul class="thumbnails">
    <li class="thumbnail">
      <%= image_tag(@product.image.url) %>
    </li>
  </ul>
<% end %>

<% if @product.taobao_url %>
  <p>
    <%= link_to '淘宝链接', @product.taobao_url, target: '_blank' %>
  </p>
<% end %>

<%= content_tag(:pre, @product.description) unless @product.description.blank? %>

<%= link_to '修改', edit_admin_product_path(@product), class: 'btn' %>
<%= link_to '返回', admin_products_path, class: 'btn' %>
